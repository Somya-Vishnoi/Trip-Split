# Trip Split

User inputs destination, group size, and total budget. System fetches hotels, restaurants, and free attractions via Overpass API (zero cost), then uses DP to find the optimal combination of stays + meals + activities that maximizes experience within budget, split fairly across group members.
